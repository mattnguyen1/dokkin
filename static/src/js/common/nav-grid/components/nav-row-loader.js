import React from "react";

const NavRowLoader = props => (
  <li className="nav-row loader">
    <div className="nav-row-title-loader" />
    <div className="nav-row-loader-text first" />
  </li>
);

export default NavRowLoader;
